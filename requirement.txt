# Core testing & linting tools
pytest
flake8
black

# Add your backend dependencies here
# Example Django dependencies (if your backend is Django-based):
# Django>=4.2,<5.0
# djangorestframework
# gunicorn

# Example ML packages (if used in backend):
# numpy
# pandas
# scikit-learn

# Add any other libraries your backend uses:
# requests
# python-dotenv

Django>=4.2
djangorestframework
psycopg[binary]              # postgres driver (or mysqlclient for MySQL)
django-environ               # env vars
django-cors-headers
django-storages[boto3]       # for S3 media
boto3
celery[redis]
redis
gunicorn
pytest-django                # for testing
