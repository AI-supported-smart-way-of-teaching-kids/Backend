name: CI
on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  backend:
    uses: AI-supported-smart-way-of-teaching-kids/Backend/.github/workflows/backend-ci.yml@main
    with:
      python-version: '3.13.2'


