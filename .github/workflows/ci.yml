name: CI

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      # ... checkout step ...
      - uses: actions/setup-python@v4
        # FIX: Align 'with:' two spaces deeper than '- uses:'
        with:
          python-version: '3.13.2'
      # ... subsequent run steps ...

